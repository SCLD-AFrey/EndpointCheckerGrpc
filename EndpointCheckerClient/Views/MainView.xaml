<UserControl 
    x:Class="EndpointCheckerClient.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm" 
    xmlns:ViewModels="clr-namespace:EndpointCheckerClient.ViewModels"
    mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="300"
    DataContext="{dxmvvm:ViewModelSource Type=ViewModels:MainViewModel}"
    xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"></ColumnDefinition>
            <ColumnDefinition Width="1*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="22"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
 
        <dx:SimpleButton Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="0" Content="Process" Command="{Binding ProcessButtonCommand}"></dx:SimpleButton>
        <TextBox Grid.Column="0" Grid.Row="1" Text="{Binding JsonList}"></TextBox>
        <TextBlock Grid.Column="2" Grid.Row="1" Text="{Binding TextOut}"></TextBlock>


    </Grid>
</UserControl>
